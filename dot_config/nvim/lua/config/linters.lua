local linters = {
  "stylelint", -- css
  "eslint", -- ts, tsx, js, jsx
  "jsonlint", -- json
  "htmlhint", -- html
  "phpcs", -- php
  "ruff", -- python
  "markdownlint", -- markdown
  "sqlfluff", -- sql,
}

return linters
